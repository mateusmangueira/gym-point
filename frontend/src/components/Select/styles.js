import styled from 'styled-components';
import { Select as Slct } from '@rocketseat/unform';

export const Select = styled(Slct)`
	width: 200px;
	height: 45px;
	background: none;
	border: 1px solid #ddd;
	border-radius: 4px;
	padding: 15px;
`;
